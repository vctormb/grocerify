type User {
  id: ID! @unique
  name: String!
  email: String @unique
  password: String!
}

type Product {
  id: ID! @unique
  imageUrl: String!
  title: String!
  price: Float!
}

# type Post {
#   id: ID! @unique
#   title: String!
#   published: Boolean! @default(value: "false")
#   author: User
# }
