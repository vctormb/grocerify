# import Product, ProductCreateInput, Order, OrderedProduct from "./prisma/generated/prisma-client/prisma.graphql"

type Query {
  products(skip: Int, first: Int): [Product!]!
  order: Order!
}

type Mutation {
  login(email: String!, password: String!): AuthPayload
  signup(name: String!, email: String!, password: String!): AuthPayload
  createProduct(data: ProductCreateInput): Product!
  createOrderedProduct(productId: ID!, quantity: Int!): Order!
  updateOrderedProduct(orderedProductId: ID!, quantity: Int!): Order!
  deleteOrderedProduct(productId: ID!): Order!
}

type User {
  id: ID!
  email: String
  name: String!
}

type AuthPayload {
  token: String!
  user: User!
}

type Product {
  id: ID!
  imageUrl: String!
  title: String!
  price: Float!
  userOrderedProduct: OrderedProduct
}